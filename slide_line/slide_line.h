#ifndef SLIDE_LINE_H
#define SLIDE_LINE_H

#include <stddef.h>

/* Définir les macros pour les directions */
#define SLIDE_LEFT /* TON CODE : quelle valeur ? 0 ou 1 ? */
#define SLIDE_RIGHT /* TON CODE : quelle valeur ? 0 ou 1 ? */

/* Prototype de ta fonction */
int slide_line(int *line, size_t size, int direction);

#endif
